export default defineAppConfig({
  // 빌드 시점에 결정되는 공개 토큰, 테마 변형, 제목 및 민감하지 않은 모든 프로젝트 구성과 같은 웹사이트 구성
  title: 'Hello Nuxt',
  theme: {
    dark: true,
    colors: {
      primary: '#ff0000'
    }
  }
});
